<UserControl x:Class="gentech_services.Views.Pages.UserManagementPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.Pages"
             xmlns:userControl="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="850">
    <Grid MaxWidth="800">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
            <RowDefinition  Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Background="#EFEFEF" Padding="12,5" CornerRadius="8" >
            <Grid  VerticalAlignment="Center" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="30*"/>

                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="User Management"  FontWeight="Bold" FontSize="30"/>
                <TextBox x:Name="TxtProductSearch" Grid.Column="2" Style="{StaticResource OutlineTextBox}"  Tag="Search by name or ID" Height="30" FontSize="12" BorderBrush="#000000" />
            </Grid>
        </Border>
        
        <userControl:UserManagementTable Grid.Row="2" />
        <userControl:AddUserModal Grid.Row="4" />

    </Grid>
</UserControl>
