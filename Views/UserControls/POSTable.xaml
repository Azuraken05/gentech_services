<UserControl x:Class="gentech_services.Views.UserControls.POSTable"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500"
             
             xmlns:control="clr-namespace:gentech_services.Views.Controls">
    <Grid  Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderThickness="0,0,0,1" BorderBrush="#000000"  >
            <Grid Grid.Row="0" Margin="0,0,25,0">
                <Grid.Resources>
                    <Style TargetType="TextBlock">
                        <Setter Property="Padding" Value="4"/>
                        <Setter Property="FontSize" Value="16"/>
                        <Setter Property="Foreground" Value="#828282"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                    </Style>
                </Grid.Resources>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.5*"/>
                    <ColumnDefinition Width="3*"/>
                    <ColumnDefinition Width="70"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>

                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="#" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="Product" TextWrapping="Wrap" TextAlignment="Center" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="2" Text="Quantity" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="3" Text="Subtotal" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="4" Text="Action" VerticalAlignment="Center"/>

            </Grid>
        </Border>
        <Border Grid.Row="2" CornerRadius="0,0,14,14" ClipToBounds="True" VerticalAlignment="Stretch" >
            <ScrollViewer HorizontalScrollBarVisibility="Auto" Style="{StaticResource CustomScrollViewer}">
                <ItemsControl ItemsSource="{Binding ProductOrders}" d:ItemsSource="{d:SampleData ItemCount=5}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource AlternatingRow}" Padding="4" Background="White" Margin="0,0,5,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.5*"/>
                                        <ColumnDefinition Width="3*"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="1*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.Resources>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Padding" Value="4"/>
                                            <Setter Property="FontSize" Value="12"/>
                                            <Setter Property="HorizontalAlignment" Value="Center"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </Grid.Resources>

                                    <TextBlock Grid.Column="0" Text="1" />
                                    <TextBlock Grid.Column="1" Text="AMD Ryzen 5 5600" TextWrapping="Wrap" TextAlignment="Center"/>
                                    <local:NumericUpDown Grid.Column="2" Height="20"/>
                                    <TextBlock Grid.Column="3" Text="₱ 200" />
                                    <control:IconBtn Grid.Column="4" Style="{StaticResource OutlineIconButton}" Background="#D22B2B" Padding="4" Margin="0,0,0,0" FontSize="12" Foreground="White" IconImg="pack://application:,,,/gentech_services;component/Resources/Assets/Icons/icnTrash.png"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
