<UserControl x:Class="gentech_services.Views.UserControls.NumericUpDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="20" >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20"/>
        </Grid.ColumnDefinitions>

        <Button Grid.Column="0" Style="{StaticResource OutlineButton}" Content="-" FontSize="16" Padding="0,0,0,4" Tag="0" Background="#80000000" Foreground="White" VerticalContentAlignment="Center" VerticalAlignment="Center" Click="Minus_Click"/>
        <TextBox Grid.Column="1"  x:Name="itemQty" MinWidth="30" MaxWidth="35" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" FontSize="12" BorderThickness="1" TextAlignment="Center" Loaded="itemQty_Loaded" />
        <Button Grid.Column="2" Style="{StaticResource OutlineButton}"  Content="+" FontSize="16" Padding="0,0,0,4" Tag="0" Background="#80000000" Foreground="White" VerticalContentAlignment="Center" Click="Plus_Click"/>
    </Grid>
</UserControl>
