<UserControl x:Class="gentech_services.Views.UserControls.AlertItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignWidth="200"
             x:Name="root">
    <Border Background="#D4183D" Height="19.5" MinWidth="120" CornerRadius="6.75" Padding="8,2" Margin="0,0,10,10">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" >
            <TextBlock Text="{Binding TextName, ElementName=root}" FontSize="10" Foreground="White"/>
            <TextBlock Text="{Binding TextStock, ElementName=root,  StringFormat={}({0} left)}" FontSize="10" Margin="5,0,0,0" Foreground="White"/>
            
        </StackPanel>
    </Border>
</UserControl>
