<UserControl x:Class="gentech_services.Views.UserControls.EditProductModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="800">
    <UserControl.Resources>
       
        <Style TargetType="TextBox" x:Key="EditTextBox" BasedOn="{StaticResource OutlineTextBox}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderBrush" Value="Black"/>
            
        </Style>
        <Style TargetType="ComboBox" x:Key="EditComboBox" BasedOn="{StaticResource OutlineComboBox}">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="BorderBrush" Value="Black"/>
            
        </Style>
        
    </UserControl.Resources>
    <Border MaxWidth="600" MinWidth="235" MinHeight="400" Background="#FFFFFF" CornerRadius="5" BorderBrush="#000000" BorderThickness="1" Padding="20" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Edit Product" FontWeight="Bold" FontSize="24"/>
            <Border Grid.Row="1"  VerticalAlignment="Stretch" Margin="0,10,0,0" >
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="20" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="Margin" Value="0,0,0,3"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                            <Style TargetType="TextBox">
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="BorderBrush" Value="#000000"/>
                            </Style>
                        </Grid.Resources>
                        <StackPanel Orientation="Vertical" Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Name "/>
                            <TextBox x:Name="TxtName" Style="{StaticResource EditTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Category "/>
                            <ComboBox x:Name="CategoryComboBox" Style="{StaticResource EditComboBox}"
                                       MaxDropDownHeight="150"/>
                            <TextBlock Text="Price "/>
                            <TextBox x:Name="TxtPrice" Style="{StaticResource EditTextBox}" Margin="0,0,0,10"/>
                            
                            
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Grid.Column="2" >
                            
                            <TextBlock Text="Stock "/>
                            <TextBox x:Name="TxtStock"  Style="{StaticResource EditTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="SKU "/>
                            <TextBox x:Name="TxtSKU"  Style="{StaticResource EditTextBox}" Margin="0,0,0,10"/>
                        </StackPanel>
                        
                    </Grid>
             
                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Description" FontSize="12" Grid.Row="0" FontWeight="Bold" Margin="0,0,0,3"/>
                        <TextBox x:Name="TxtDescription" Style="{StaticResource OutlineTextBoxMultiline}"  Grid.Row="1" BorderBrush="Black"/>
                    </Grid>
                    
                </Grid>

            </Border>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0" >
                <Button x:Name="BtnSave" Content="Save"  FontSize="12" Padding="8,4" Style="{StaticResource OutlineButton}" Background="#0000FF" Foreground="White"  Width="70"/>
                <Button  x:Name="BtnCancel" Content="Cancel"  FontSize="12" Padding="8,4" Style="{StaticResource OutlineButton}" Background="#FFFFFF" Width="70" Margin="10,0,0,0"/>

            </StackPanel>

        </Grid>
    

    </Border>
</UserControl>
