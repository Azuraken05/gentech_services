<UserControl x:Class="gentech_services.Views.UserControls.EditProductModal"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:gentech_services.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800">
    <Border MaxWidth="500" MinWidth="235" MinHeight="241" Background="#FFFFFF" CornerRadius="5" BorderBrush="#000000" BorderThickness="1" Padding="20" >
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="Edit Product" FontWeight="Bold" FontSize="18"/>
            <Border Grid.Row="1"  VerticalAlignment="Stretch" Background="#EEEEEEEE" CornerRadius="5" Margin="0,5,0,0" Padding="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                            <Style TargetType="TextBox">
                                <Setter Property="FontSize" Value="12"/>
                                <Setter Property="BorderBrush" Value="#000000"/>
                            </Style>
                        </Grid.Resources>
                        <StackPanel Orientation="Vertical" Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Name "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Category "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Price "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Stock "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Grid.Column="2" VerticalAlignment="Center">
                            <TextBlock Text="Name "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Category "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Price "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                            <TextBlock Text="Stock "/>
                            <TextBox Style="{StaticResource OutlineTextBox}" Margin="0,0,0,10"/>
                        </StackPanel>
                        
                    </Grid>
             
                    <Grid Grid.Row="2" Margin="0,10,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Text="Description" FontSize="12" Grid.Row="0"/>
                        <TextBox Style="{StaticResource OutlineTextBoxMultiline}"  Grid.Row="1"/>
                    </Grid>
                    
                </Grid>

            </Border>
            <StackPanel Orientation="Horizontal">
                <Button />
            </StackPanel>
        </Grid>
    

    </Border>
</UserControl>
